1、列表 list
[],最常用数据类型，存放同族元素
2、dictionary
以,隔开，键值对应，且无序。{}
3、元祖
列表的一种，不可变的列表。

4、集合、set
去重，{},不可重复

一组列表
在C/programfile 下打开cmder，
 
 
把数据装到容器中
通过split将文本片段分割成一个个元素的列表。
split的窍门：找到一个可以按照目的来分割大段文字的 缝隙，例如：换行符、空格、标点符号，比较特殊的会是特殊字符。

实战：整理容器以完成数据整理
1、去掉不想要的元素，
2、挑出想要的元素
3、通过循环，将干净的元素挑出来

容器索引：
正向索引、逆向索引
0 1    2  3 
-4 -3 -2 -1
ship=[‘z','e','p','l','i','n']
['z','e','p']=ship[0:3]
tips:
